# GPU cluster requirements (SLURM + conda/venv)
# Install torch FIRST with the correct CUDA index, then run this file with --no-deps:
#
#   conda mode (drone_detect_gpu.slurm):
#     pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu121
#     pip install -r requirements-cluster.txt --no-deps
#
#   venv mode (yolov5_visdrone_train.slurm):
#     pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu121
#     pip install -r requirements-cluster.txt

# Base (torch/torchvision installed separately — see above)
gitpython>=3.1.30
matplotlib>=3.3
numpy>=1.23.5
opencv-python>=4.1.1
pillow>=10.3.0
psutil
PyYAML>=5.3.1
requests>=2.32.2
scipy>=1.4.1
thop>=0.1.1
tqdm>=4.66.3
ultralytics==8.0.100  # pinned to old API (ultralytics.yolo.*)

# Cluster runtime deps that pip misses with --no-deps
pytz
python-dateutil
urllib3
certifi
charset-normalizer
idna
pyparsing
contourpy
cycler
fonttools
kiwisolver
packaging
gitdb

# Plotting
pandas>=1.1.4
seaborn>=0.11.0

# Deploy
setuptools>=70.0.0
