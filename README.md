# Drone-Detect-Benchmark

**Enhancing Small Object Detection in UAV Imagery Using an Optimized Attention-Driven YOLO Model**  
A Benchmarking Study on VisDrone2019  
PhD Coursework — Deep Learning & Deep Learning for Visual Recognition  
Second Semester (2024–2025 Even)  
Group: 24DS611 & 24DS736

---

## 📌 Project Overview

This repository contains code and configuration for our benchmarking study focused on improving small object detection in UAV (Unmanned Aerial Vehicle) imagery. We extend the YOLOv5 model by integrating attention mechanisms (such as Swin Transformer blocks) and evaluate performance on the VisDrone2019 dataset.

The aim is to optimize detection accuracy for small and occluded objects commonly found in aerial surveillance footage using advanced feature representation techniques.

---

## 🧠 Key Contributions

- Benchmarking traditional YOLOv5 models against Swin-Transformer-augmented variants.
- Fine-tuned configurations for small object detection in the VisDrone2019 dataset.
- Comparative training results and performance analysis.

---

## 🗃️ Dataset

We use the **VisDrone2019-DET** dataset (~2.3 GB). The repo includes a download + conversion script that handles everything automatically.

### Download and convert

```bash
python download_visdrone.py --path /your/dataset/path
```

This will:
1. Download train, val, and test-dev splits from Ultralytics mirrors
2. Extract and remove the zip files
3. Convert VisDrone annotations to YOLO format (generates `labels/`)
4. Print a summary of image and label counts

**Options:**

| Flag | Description |
|---|---|
| `--path PATH` | Destination directory (required) |
| `--skip-download` | Convert only — skip download/extract |
| `--skip-convert` | Download/extract only — skip annotation conversion |
| `--splits A B C` | Override which splits to process |

**Known paths:**

| Machine | Path |
|---|---|
| Local (OMEN) | `/mnt/mydrive/ajith/data_set/VisDrone` |
| Cluster | `/dist_home/ak_ajithkumar/ondemand/ajith_work/VisDrone` |

### After downloading

Update `data/VisDrone.yaml` with your local path:

```yaml
path: /your/dataset/path
```

The corresponding cluster config is at `data/VisDrone_cluster.yaml`.

### Expected structure

```
VisDrone/
├── VisDrone2019-DET-train/
│   ├── images/        # 6471 images
│   ├── annotations/
│   └── labels/        # generated by download_visdrone.py
├── VisDrone2019-DET-val/
│   ├── images/        # 548 images
│   └── labels/
└── VisDrone2019-DET-test-dev/
    ├── images/        # 1610 images
    └── labels/
```

### Training

```bash
python train.py --img 640 --batch 64 --epochs 300 --data VisDrone.yaml --cfg models/yolov5n.yaml --name yolov5_n_no_swin

python train.py --img 640 --batch 16 --epochs 300 --data VisDrone.yaml --cfg models/yolov5_swin.yaml --name yolov5_15_100

python train.py --img 640 --batch 16 --epochs 300 --data VisDrone.yaml --cfg models/yolov5s_swin.yaml --name yolov5_s_swin
```

---

## Environment Setup

Requirements: Ubuntu 22.04, NVIDIA GPU (driver 570+), CUDA 12.1.

### Fresh machine (includes NVIDIA driver install)

```bash
git clone <repo-url>
cd drone-detect-benchmark
bash setup_env.sh
```

The script will install the NVIDIA driver and then prompt a reboot. After rebooting:

```bash
bash setup_env.sh --no-driver
conda activate yolov5
```

### Driver already installed

```bash
bash setup_env.sh --no-driver
conda activate yolov5
```

`setup_env.sh` handles everything: system packages, Miniconda, the `yolov5` conda env (Python 3.10), PyTorch 2.5.1+cu121, and all project dependencies including `timm` for the Swin Transformer backbone.

### Sanity-check training run

```bash
python train.py --img 640 --batch 4 --epochs 1 \
  --data data/VisDrone.yaml --cfg models/yolov5s_swin2.yaml \
  --device 0 --seed 42 --loss-log-interval 10 \
  --name swin_small_sanity --project runs/sanity \
  --scale-aware-loss --resolution-weighting \
  --scale-alpha 1.5 --resolution-beta 3.0 1.0 0.4
```